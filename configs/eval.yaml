# @package _global_

defaults:
  - _self_
  - data: silicon # choose datamodule with `test_dataloader()` for evaluation
  - model: nequip
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing model path is necessary for evaluation
config_path: ???

# passing checkpoint path is necessary for evaluation
ckpt_path: ???

predict:
